import 'package:flutter/material.dart';

const kBorderDesign = OutlineInputBorder(
    borderRadius: BorderRadius.all(Radius.circular(50)),
    borderSide: BorderSide(color: Colors.orange, width: 4));
